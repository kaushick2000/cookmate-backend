# Application Configuration
spring.application.name=cookmate-backend
server.port=8080

# MySQL Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cookmate
spring.datasource.username=root
spring.datasource.password=Jo.marley@2406
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
# Updated Hibernate dialect to the recommended MySQLDialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
# Explicitly disable Open-in-View to avoid database queries during view rendering
spring.jpa.open-in-view=false

# JWT Configuration
jwt.secret=5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
jwt.expiration=86400000

# Email Configuration (Gmail example)
# IMPORTANT: For Gmail, you need to:
# 1. Enable 2-Factor Authentication on your Google account
# 2. Generate an App Password at: https://myaccount.google.com/apppasswords
# 3. Use that App Password (16 characters) here instead of your regular password
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=cookmate25@gmail.com
spring.mail.password=tmeu fckk qrkj xvmq
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
spring.servlet.multipart.file-size-threshold=2KB

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:5173

# Logging
logging.level.com.cookmate=DEBUG
logging.level.org.springframework.security=DEBUG

# Google AI Configuration
google.ai.api.key=AIzaSyABrOVXup2ANGtA6allX7JSJzCzNV-S5VM
google.ai.enabled=true

# OAuth2 Configuration
# NOTE: Replace these with actual Google OAuth2 credentials from Google Cloud Console
# Go to: https://console.cloud.google.com/apis/credentials
# Create OAuth 2.0 Client IDs and replace the values below
spring.security.oauth2.client.registration.google.client-id=REPLACE_WITH_YOUR_GOOGLE_CLIENT_ID
spring.security.oauth2.client.registration.google.client-secret=REPLACE_WITH_YOUR_GOOGLE_CLIENT_SECRET
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/oauth2/callback/{registrationId}

# OAuth2 App Configuration
app.oauth2.authorizedRedirectUri=http://localhost:3000/oauth2/redirect